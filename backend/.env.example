# 汤探局后端环境变量示例
# 使用 python-dotenv 加载（可选）

# 服务器配置
HOST=0.0.0.0
PORT=8000
LOG_LEVEL=INFO

# 房间 TTL 秒（默认 30 分钟）
ROOM_TTL_SECONDS=1800

# 房间 GC 间隔秒（默认 60 秒）
ROOM_GC_INTERVAL_SECONDS=60

# 开发模式：开启 /api/dev/reset_cards 和 /api/dev/reload_data
DEV_MODE=true
