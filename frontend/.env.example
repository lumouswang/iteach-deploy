# 汤探局前端环境变量示例
# 复制为 .env.development.local / .env.production 后填入实际值

# 后端 API base URL（默认走 vite proxy /api）
VITE_API_BASE=/api

# WebSocket URL（默认 ws://localhost:5173/ws/{roomId}）
# 生产环境必须设置成 wss:// 与 HTTPS 配套
VITE_WS_BASE=
